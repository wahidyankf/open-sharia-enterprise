/** Border radius token values for programmatic access. */
export const radius = {
  DEFAULT: "0.5rem",
  lg: "var(--radius)",
  md: "calc(var(--radius) - 2px)",
  sm: "calc(var(--radius) - 4px)",
} as const;
