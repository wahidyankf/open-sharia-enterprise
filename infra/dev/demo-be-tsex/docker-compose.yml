# Local Development Environment for Demo Backend (TSEX — TypeScript/Effect)
# Mutually exclusive with other demo-be backends — all bind port 8201.
# Do not run multiple backend stacks simultaneously.

services:
  demo-be-tsex-db:
    image: postgres:17-alpine
    container_name: demo-be-tsex-db
    environment:
      POSTGRES_DB: demo_be_tsex
      POSTGRES_USER: ${POSTGRES_USER:-demo_be_tsex}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-demo_be_tsex}
    ports:
      - "5432:5432"
    volumes:
      - demo-be-tsex-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-demo_be_tsex} -d demo_be_tsex"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - demo-be-tsex-network

  demo-be-tsex:
    build:
      context: .
      dockerfile: Dockerfile.be.dev
    container_name: demo-be-tsex

    # Mount app source
    volumes:
      - ../../../apps/demo-be-tsex:/workspace:rw

    ports:
      - "8201:8201"

    depends_on:
      demo-be-tsex-db:
        condition: service_healthy

    environment:
      - PORT=8201
      - DATABASE_URL=postgresql://demo_be_tsex:demo_be_tsex@demo-be-tsex-db:5432/demo_be_tsex
      - APP_JWT_SECRET=${APP_JWT_SECRET:-change-me-in-dev-only-not-for-production}

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8201/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

    restart: unless-stopped
    networks:
      - demo-be-tsex-network

networks:
  demo-be-tsex-network:
    driver: bridge
    name: demo-be-tsex-network

volumes:
  demo-be-tsex-db-data:
