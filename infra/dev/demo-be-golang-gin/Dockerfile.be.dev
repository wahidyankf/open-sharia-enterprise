# Development Docker Image for Demo Backend (GOGN — Go/Gin)
# Uses Go 1.24 Alpine for building and running the application.

FROM golang:1.24-alpine

# Install curl for health checks
RUN apk add --no-cache curl

WORKDIR /workspace

CMD ["go", "run", "./cmd/server"]
