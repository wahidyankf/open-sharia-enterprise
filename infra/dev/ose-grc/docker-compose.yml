services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ose_grc_dev
      POSTGRES_USER: ose_grc
      POSTGRES_PASSWORD: ose_grc
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "ose_grc", "-d", "ose_grc_dev"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  ose-grc-be:
    build:
      context: .
      dockerfile: Dockerfile.be.dev
    ports:
      - "8302:8302"
    environment:
      DATABASE_URL: "postgresql://ose_grc:ose_grc@postgres:5432/ose_grc_dev"
      OPENROUTER_API_KEY: "${OPENROUTER_API_KEY:-}"
      OPENROUTER_MODEL: "${OPENROUTER_MODEL:-openrouter/auto}"
      OPENROUTER_BASE_URL: "${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}"
      ASPNETCORE_URLS: "http://+:8302"
    volumes:
      - ../../../apps/ose-grc-be:/workspace:rw
      - ../../../specs/apps/ose-grc/behavior/be/gherkin:/specs/apps/ose-grc/behavior/be/gherkin:ro
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8302/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s

  ose-grc-web:
    build:
      context: ../../..
      dockerfile: apps/ose-grc-web/Dockerfile
      args:
        OSE_GRC_BE_URL: http://ose-grc-be:8302
    ports:
      - "3300:3300"
    environment:
      OSE_GRC_BE_URL: "http://ose-grc-be:8302"
    depends_on:
      ose-grc-be:
        condition: service_healthy
