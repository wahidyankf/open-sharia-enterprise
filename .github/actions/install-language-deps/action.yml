name: "Install Language Dependencies"
description: "Install language-specific dependencies needed before build/lint/test"
inputs:
  language:
    required: true
    description: "Language runtime (golang, dotnet, python, typescript)"
  backend-name:
    required: false
    default: ""
    description: "Backend project name (used to scope dependency installation)"
runs:
  using: "composite"
  steps:
    - name: No-op
      shell: bash
      run: echo "No language-specific pre-install required"
