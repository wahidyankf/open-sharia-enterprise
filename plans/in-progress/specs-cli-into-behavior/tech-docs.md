# Tech Docs: Consolidate CLI Specs Under `behavior/`

## Path Changes Per App

### oseplatform

| Before                        | After                                  |
| ----------------------------- | -------------------------------------- |
| `specs/apps/oseplatform/cli/` | `specs/apps/oseplatform/behavior/cli/` |

**Go test path string** (both `links_check_test.go` and `links-check.integration_test.go`):

```go
// Before
return filepath.Join(filepath.Dir(f), "../../../specs/apps/oseplatform/cli/gherkin")
// After
return filepath.Join(filepath.Dir(f), "../../../specs/apps/oseplatform/behavior/cli/gherkin")
```

**`apps/oseplatform-cli/project.json` — `spec-coverage` target**:

```json
// Before
"command": "... spec-coverage validate specs/apps/oseplatform/cli/gherkin apps/oseplatform-cli ...",
"inputs": ["{workspaceRoot}/specs/apps/oseplatform/cli/gherkin/**/*.feature", ...]

// After
"command": "... spec-coverage validate specs/apps/oseplatform/behavior/cli/gherkin apps/oseplatform-cli ...",
"inputs": ["{workspaceRoot}/specs/apps/oseplatform/behavior/cli/gherkin/**/*.feature", ...]
```

Note: `test:quick` and `test:integration` inputs use
`{workspaceRoot}/specs/apps/oseplatform/**/*.feature` (app-level glob) — no change needed.

---

### ayokoding

Identical shape to oseplatform. Replace `oseplatform` with `ayokoding` throughout.

---

### rhino

**More complex** — `behavior/cli/gherkin/` already exists with a `specs/` subfolder.

**Spec directory merge**:

```
Before:
  specs/apps/rhino/cli/gherkin/
    agents-sync.feature          (+ 17 more *.feature files)
    README.md

  specs/apps/rhino/behavior/cli/gherkin/
    README.md                    (covers specs subcommand only)
    specs/
      .gitkeep
      validate-adoption.feature  (planned)
      validate-counts.feature    (planned)
      validate-links.feature     (planned)
      validate-tree.feature      (planned)

After:
  specs/apps/rhino/behavior/cli/gherkin/
    agents-sync.feature          (moved from cli/gherkin/)
    ...all 18 *.feature files... (moved)
    README.md                    (REPLACED — comprehensive, covers all commands)
    specs/
      .gitkeep
      validate-*.feature         (unchanged — planned, stays in subfolder)
```

`specs/apps/rhino/cli/` directory removed entirely.

**`specs/` subfolder edge case — MUST CHECK**:

`spec-coverage validate` command takes a directory arg. After pointing at
`specs/apps/rhino/behavior/cli/gherkin`, if the tool recurses into `specs/`, it will
check the 4 planned `validate-*.feature` files for test coverage and fail (no
implementations exist yet).

Verification step (delivery item 3.12): run `spec-coverage` and check output. If it
fails on `validate-*.feature` files, resolve by one of:

1. Implementing stub test files for the 4 planned specs (preferred — unblocks the subcommand work).
2. Scoping spec-coverage to `behavior/cli/gherkin` non-recursively if the tool supports it.

**Go test path string** (all 36 rhino-cli test files):

```go
// Before
return filepath.Join(filepath.Dir(f), "../../../specs/apps/rhino/cli/gherkin")
// After
return filepath.Join(filepath.Dir(f), "../../../specs/apps/rhino/behavior/cli/gherkin")
```

All 36 files share the identical pattern at line 19. Sed replace handles all:

```bash
cd apps/rhino-cli
sed -i '' \
  's|specs/apps/rhino/cli/gherkin|specs/apps/rhino/behavior/cli/gherkin|g' \
  cmd/*_test.go cmd/*.integration_test.go
```

**`apps/rhino-cli/project.json` — `spec-coverage` target only**:

```json
// Before
"command": "... spec-coverage validate specs/apps/rhino/cli/gherkin apps/rhino-cli ...",
"inputs": ["{workspaceRoot}/specs/apps/rhino/cli/gherkin/**/*.feature", ...]

// After
"command": "... spec-coverage validate specs/apps/rhino/behavior/cli/gherkin apps/rhino-cli ...",
"inputs": ["{workspaceRoot}/specs/apps/rhino/behavior/cli/gherkin/**/*.feature", ...]
```

Other rhino targets use `{workspaceRoot}/specs/apps/rhino/**/*.feature` — no change.

---

## README Merge — `specs/apps/rhino/behavior/cli/gherkin/README.md`

Current file covers only the `specs` subcommand (4 planned features). After migration,
replace it with a comprehensive README that:

1. Lists all 18 implemented feature files (carry the table from `cli/gherkin/README.md`).
2. Documents the `specs/` subfolder and its 4 planned `validate-*` features.
3. Preserves conventions section and related links.

---

## Files Not Needing Changes

- `apps/oseplatform-cli/project.json` `test:quick`/`test:integration` inputs — use `specs/apps/oseplatform/**/*.feature` glob already.
- `apps/ayokoding-cli/project.json` same.
- `apps/rhino-cli/project.json` `test:quick`/`test:integration`/`test:unit` inputs — use `specs/apps/rhino/**/*.feature` glob already.
- `.nx/workspace-data/` — auto-regenerated by Nx on next run.
- `plans/done/` and `generated-reports/` — historical, no live impact.

---

## Commit Strategy

Three commits, one per app, for clean blame:

1. `refactor(specs): move oseplatform cli specs under behavior/cli`
2. `refactor(specs): move ayokoding cli specs under behavior/cli`
3. `refactor(specs): move rhino cli specs under behavior/cli`

Or one atomic commit if all tests green together:

1. `refactor(specs): consolidate all cli/ specs under behavior/cli/`
